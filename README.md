
  # Personal Site

React code for personal site. Template made by Figma Make and modified to add in my stuff

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
